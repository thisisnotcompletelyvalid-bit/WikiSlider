name: Bootstrap WikiSlider

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  bootstrap:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload refresh in progress"
